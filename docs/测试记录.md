# 测试记录

## 1.0.1 图标与文案更新

验证日期：2026-09-18。版本号 `1.0.1`，`versionCode=2`。按主目录 `Z图标矢量图.png` 中的双色 Z 轮廓与配色重绘安卓矢量图标，提供自适应前景、背景及单色主题图标。

- `./gradlew :app:assembleRelease :app:lintRelease` 通过，Lint 为 0 错误、17 条非阻断提示。
- API 36 模拟器通过 `adb install -r` 从 1.0.0 覆盖升级，浅色主题与雾蓝配色保留。
- 首次使用界面仅保留原插画和选择目录按钮；指定的标题、引导文字、格式行和隐私行均已删除。
- 书库、收藏与最近阅读的副标题仅显示漫画数量；继续阅读卡片使用「继续阅读」。
- 空列表提示为「试试其他筛选条件，」和「或收藏一本喜欢的漫画。」两行，截图确认居中。
- 设置页不再显示顶部与底部宣传文案；关于页面版本号来自构建配置。
- 模拟器截图确认新版桌面图标、设置页、空列表与继续阅读卡片正常，截图位于 `test-output/1.0.1/`。
- 发布包通过 `apksigner verify` 和 `zipalign -c -P 16 4`；签名证书与 1.0.0 相同。

交付文件：`dist/ZViewer-1.0.1-release.apk`。SHA-256：`d1599f56c08c069738f80d293dec2a903818977869bf6109fd850d0dbf5c0337`。

以下为 1.0.0 的初版验证记录。

测试日期：2026-09-18。测试工程在 WSL2 Ubuntu，JDK 17.0.20，Gradle Wrapper 8.11.1，Android SDK 36，模拟器分辨率 1080×2400。

## 自动化结果

| 检查 | 命令 / 设备 | 结果 |
| --- | --- | --- |
| Kotlin 单元测试 | `./gradlew :app:testDebugUnitTest` | 通过；自然排序、隐藏元数据过滤、缓存键、图片扩展名 |
| Android 格式集成测试 | `emulator-5554` API 34 | 8 / 8 通过 |
| Android 格式集成测试 | `emulator-5580` API 36 | 8 / 8 通过 |
| Lint | `./gradlew :app:lintDebug` | 通过；仅保留 AndroidX KTX、版本建议等非阻断提示 |
| Debug / Release 构建 | `./gradlew :app:assembleDebug :app:assembleRelease` | 通过 |
| APK 对齐 | `zipalign -c -P 16 -v 4 app-release.apk` | 通过 |
| APK 签名 | `apksigner verify --verbose --print-certs` | 通过，v2，SHA-256 `ece618f4c8b80977688e4d8d5c08cb8b1a9312845ac8f0609160cbb6e06e7afb` |

格式测试使用 `tools/generate_fixtures.py` 生成的原创样本，覆盖：CBZ / ZIP、RAR4 / CBR、RAR5 / RAR、7Z、三页 PDF、图片文件夹、JPG / JPEG / PNG / WebP / BMP / HEIF / HEIC / AVIF、APNG、Animated WebP、GIF、Animated AVIF、损坏压缩包、空压缩包、路径穿越条目和 4800×3200 大尺寸 APNG。测试确认自然页序、封面缓存、解压安全边界和动画 Drawable 均可工作。

## 设备手动验收

API 34 与 API 36 均完成以下流程：首次启动 → SAF 选择具体书库子目录 → 八本样书出现 → 搜索「海边」→ 收藏 / 最近阅读 → 打开 CBZ → 显示 / 收起浮动进度条 → 页码跳转 → 水平翻页 → 垂直连续 → 双指 / 双击缩放 → 放大、缩小、适合屏幕 → 深色主题 → 自定义颜色 → 系统边缘返回 → 强制停止后重新打开 → 恢复上次页码。每项通过。

`tools/ui_smoke.py` 可重复大部分流程。某些 Compose 版本的 UiAutomator 桥接不会把拖动事件稳定发送给 Slider；脚本在该桥接限制下自动使用同一面板的页码跳转作为备用验证，真实设备触摸拖动已在模拟器人工操作中确认。

## APK 低层检查

Release APK 仅包含 `arm64-v8a` 与 `x86_64`，原生库为 libarchive、libavif 和 AndroidX path rendering。`readelf -l` 显示三个库的 LOAD 段对齐为 `0x4000`，适配 Android 15 / 16 的 16 KB 页面设备；系统模拟器的 `getconf PAGE_SIZE` 返回 4096，另用 API 36 镜像完成格式测试。

内存观察：API 34 模拟器打开三页 CBZ 后应用 PSS 约 150 MiB，Native Heap 约 58 MiB；大图使用目标尺寸采样，未出现 OOM。实际手机的内存占用取决于屏幕尺寸、厂商解码器和图片编码。

## 已知边界

- 自动化 UI 对系统文件选择器与 Compose Slider 的跨进程点击 / 拖动依赖较强，遇到 StaleObject 时按 README 的手动流程复核。
- Windows WSL 的 `adb` 可连接两个模拟器；测试命令必须显式指定 `-s DEVICE`，否则可能将 APK 推送到错误设备。
- API 36 的新边到边行为和厂商定制文件选择器仍需在目标品牌手机上做一次人工验收。
